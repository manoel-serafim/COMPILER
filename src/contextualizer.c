#include "contextualizer.h"
#include "parser.h"

/*--[Syntax Tree Traversal]--*/
/*[path can be from front to back(symbol table) and back to front(type check)]*/

/*--[Semantics Analyser]--*/
/*Type check && Symbol Table*/
void contextualize(syntax_t_node * root){

}